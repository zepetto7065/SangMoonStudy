JPA (Java Persistence API)
- 과거 객체 저장 sql  JDBC
- mybatis 매퍼를 이용 MyBatis , JdbcTemplate
- 단순 객체를 저장할때, 개발자대신 적절한 쿼리를 생성  JPA

장점
개발 생산성
유지보수 

실무에서 어려운 이유?
예제는 테이블이 한 두개로 단순, 실무는 복잡한 객체와 테이블 사용 -> 바로 망한다

목표
객체와 테이블을 제대로 설계하고 매핑
기본 키와 외래 키 매핑
1:N , N:1 , 1:1, N:M 매핑
실무 노하우 + 성능

#내부동작방식이해
어떤 SQL, 언제 실행하는지

강의는 이론 + 라이브 코딩
강의는 메인 책은 참고서



