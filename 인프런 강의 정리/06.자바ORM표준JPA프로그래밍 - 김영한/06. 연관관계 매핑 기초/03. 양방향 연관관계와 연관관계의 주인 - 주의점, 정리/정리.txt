## 양방향 매핑시 가장 많이 하는 실수
-순수 객체 상태 골 항상 양쪽에 값을 설정하자
-연관관계 편의 메소드를 생성하자
-양방향 매핑시 무한루프를 조심하자
 ex) toString(), lombok (toSting 사용 x )
,JSON 생성 라이브러리 ( Controller에서 Entity를 반환하지 마라)
-> 엔티티는 dto(단순하게 값이 있는)로 변환해서 반환

-> 단방향 매핑만으로도 이미 연관관계 매핑은 완료해야함
-> 단방향 매핑을 잘 하고 양방향은 필요할 때 추가해야함

